import { redirect } from "next/navigation";

/** INTIENDAS password recovery retired with the product. */
export default function IntiendasForgotPasswordRetiredPage() {
  redirect("/forgot-password");
}
