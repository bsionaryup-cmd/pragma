"use client";

import { ThemeToggle } from "@/components/layout/theme-toggle";

/** @deprecated Use ThemeToggle — kept for backward compatibility. */
export function ThemeModeButton() {
  return <ThemeToggle size="sm" />;
}
