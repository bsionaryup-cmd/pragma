/** Ancho de cada columna-día (px) */
export const CALENDAR_DAY_WIDTH = 100;

/** Hueco visual entre barra de salida y de entrada el mismo día (px). */
export const CALENDAR_TURNOVER_GAP_PX = 12;

/** Altura de cada fila-propiedad (px) */
export const CALENDAR_ROW_HEIGHT = 88;

/** Sidebar izquierda de propiedades (px) */
export const CALENDAR_SIDEBAR_WIDTH = 280;

/** Header de días (px) */
export const CALENDAR_DAY_HEADER_HEIGHT = 52;

/** Toolbar superior (px) */
export const CALENDAR_TOOLBAR_HEIGHT = 48;
