import type { NormalizedLead } from "@/lib/apify/types";

/** Airbnb provider stub — returns NormalizedLead[] when implemented. */
export async function scrapeAirbnb(_searchTerm: string): Promise<NormalizedLead[]> {
  return [];
}
