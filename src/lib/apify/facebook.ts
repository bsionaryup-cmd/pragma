import type { NormalizedLead } from "@/lib/apify/types";

/** Facebook provider stub — returns NormalizedLead[] when implemented. */
export async function scrapeFacebook(_searchTerm: string): Promise<NormalizedLead[]> {
  return [];
}
