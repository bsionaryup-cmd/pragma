import type { NormalizedLead } from "@/lib/apify/types";

/** Instagram provider stub — returns NormalizedLead[] when implemented. */
export async function scrapeInstagram(_searchTerm: string): Promise<NormalizedLead[]> {
  return [];
}
