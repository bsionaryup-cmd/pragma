import type { NormalizedLead } from "@/lib/apify/types";

/** LinkedIn provider stub — returns NormalizedLead[] when implemented. */
export async function scrapeLinkedIn(_searchTerm: string): Promise<NormalizedLead[]> {
  return [];
}
